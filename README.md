# Projet : Amélioration d'une applications de gestion des commandes d'un *food truck*

## Le projet peut être exécuté de la façon suivante:

```bash
mvn clean package
java -jar <nom du fichier>.jar
```
